<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval" >

    <!-- 描边，边框 -->
    <stroke
        android:width="@dimen/dp3"
        android:color="@color/gray_a6"/>

    <!-- 设置圆角半径 -->
    <!--<corners android:radius="@dimen/dp130"/>-->

    <!-- 填充的颜色 -->
    <solid android:color="@color/transparent" />

    <!-- 大小 -->
    <size
        android:width="@dimen/dp100"
        android:height="@dimen/dp40"/><!-- 宽度和高度 -->

</shape>